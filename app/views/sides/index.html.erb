<h1>Listing sides</h1>

<table>
  <tr>
    <th>Name</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @sides.each do |side| %>
  <tr>
    <td><%= side.name %></td>
    <td><%= link_to 'Show', side %></td>
    <td><%= link_to 'Edit', edit_side_path(side) %></td>
    <td><%= link_to 'Destroy', side, :confirm => 'Are you sure?', :method => :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Side', new_side_path %>
